En este archivo documentaremos los cambios realizados en los conjuntos de datos Chronic Kidney.

# Kidney1 336 chronic+kidney+disease

    Cargamos las librerias
    Descargamos el dataset y lo guardamos como Kidney1
    Vemos si hay duplicados
    Trabajamos con los datos faltantes
    Creamos columnas nuevas para los valores faltantes
    Los valores faltantes pequenios los imputamos con la moda
    Normalizamos
    Eliminamos las filas que tienen variables object
    Descargamos el dataset y lo guardamos como Kidney1
    Creamos una tabla que describe las columnas
