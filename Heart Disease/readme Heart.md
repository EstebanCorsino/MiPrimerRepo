En este archivo documentaremos los cambios realizados en los conjuntos de datos cardiacos
# Heart1 Cardio train dataset duianova cardiovascular-disease-dataset

    Cargamos las librerias
    Descargamos la base de datos y la llamamos Heart1
    Vemos el tamanio del dataset en este caso es (70000, 1)
    Vemos si hay datos faltantes
    Vemos informacion general de las columnas con .info()
    Verificamos  si no hay nulos
    Vemos si hay duplicados
    Le pedimos a la ia que nos haga una descripcion de cada columna
    Descargamos nuestro dataset y lo guardamos como Heart1_processed.csv

# Heart2 heart disease dataset 45 heart disease 

    Cargamos las librerias
    descargamos el dataset le asignamos el nombre de Heart2
    Vemos el tamanio del dataset en este caso (303, 14)
    Vemos si hay datos faltantes
    Completamos datos nulos se agrego una columna para usar one-hot encoding y en otra se uso la moda
    Vemos si hay duplicados
    Descargamos la dataset y guardamos como Heart2
    Le pedimos a la ia que nos generara una tabla con la descripcion de las columnas

# Heart3 cardio disease dataset dzz48mvjht

    Cargamos las librerias
    Cargamos la base de datos y guardamos como Heart3
    vemos si hay datos faltantes
    Vemos el tamanio del dataset
    vemos una descripcion de las variables
    Vemos si hay duplicados
    Descargamos el csv
    Le pedimos a la ia que nos generara una tabla con la descripcion de las columnas



# Heart4  heart 519 failure heart+failure+clinical+records
    Cargamos las librerias
    descargamos el dataset le asignamos el nombre de Heart4
    Vemos el tamanio del dataset en este caso (299, 13)
    Vemos si hay datos faltantes, en este caso no hay
    Veamos si hay duplicados, en este caso no hay
    En este caso no hubo que normalizar pues las variables eran int o float
    Descargamos el dataset y guardamos como Heart4
    Le pedimos a la ia que nos generara una tabla con la descripcion de las columnas
# Heart5 personal key indicators of heart disease
    Cargamos las librerias
    descargamos el dataset le asignamos el nombre de Heart5
    Vemos el tamanio del dataset en este caso (319795, 18)
    Vemos si hay datos faltantes, en este caso no hay
    Vemos si hay duplicados, en este caso si hay
    Limpiamos los duplicados
    Descargamos el dataset y guardamos como Heart5
    En este caso no hubo que normalizar pues las variables eran int o float
    Le pedimos ala ia que nos generara una tabla con el nombre de las variables de laas columnas