En este archivo documentaremos los cambios realizados en los conjuntos de datos Pulmonary.

# Pulmo1 support2 dataset 880 support2

    Cargamos las librerias
    Cargamos la base de datos
    Le asignamos el nombre de Pulmo1
    Vemos el tamanio del dataset
    Vemos si hay duplicados, en este caso no hay
    Vemos datos faltantes
    Revisamos las variables que tienen datos faltantes
    Comenzamos con los datos nulos
    Revisamos los tipos de variables para tomar la decision si agregar nueva columna o aplicar moda, mediana o media.
    aplicamos usamos KNN Imputer: rellena valores faltantes buscando pacientes similares.
    Normalizamos los datos que esten en modo object
    Agregamos columnas necesarias para usar One-Hot Encoding
    Revisamos si ya no hay datos faltantes y si solo tenemos variables int o float
    Descargamos el dataset y guardamos como Pulmo1
    Le pedimos a la ia que nos genere una tabla con una descripcion de las columnas

# Pulmo2 heart disease combined kamilpytlak personal-key-indicator-of-heart

    Cargamos las librerias
    Cargamos la base de datos
    Le asignamos el nombre de Pulmo2
    Vemos el tamanio del dataset
    Vemos datos faltantes
    Vemos si hay duplicados
    Eliminamos duplicados
    Vemos un resumen del info para ver el tipo de variables
    Normalizamos
    Descargamos el dataset y lo guardamos en Pulmo2
    Le pedimos a la ia que nos genere una tabla que desfriba las variables
